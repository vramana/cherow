import { parse, parseSource, parseModule, parseScript, validateRegExp } from './parser/parser';
export declare const version = "1.6.8";
export { parse, parseSource, parseModule, parseScript, validateRegExp };
